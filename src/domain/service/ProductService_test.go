package service_test

import (
	"testing"
)

func TestProductService_FindById(t *testing.T) {
	// Crie uma instância do serviço ProductService
	// productService := NewProductService()

	// Crie um teste case com um ID válido e o resultado esperado
	// testCase := struct {
	// 	id             int
	// 	expectedResult *model.Product
	// 	expectedError  error
	// }{
	// 	id: 1,
	// 	// expectedResult: &model.Product{ID: 1, Name: "Product 1"},
	// 	expectedError: nil,
	// }
	// fmt.Println(testCase.expectedError)

	// mock.NewMockIProductRepository(f.ctrl)

	// Chame o método FindById do serviço
	// product, err := service.ProductService.FindById(testCase.id)

	// Verifique se o resultado está de acordo com o esperado
	// if product.ID != testCase.expectedResult.ID {
	// 		t.Errorf("ID esperado: %d, ID retornado: %d", testCase.expectedResult.ID, product.ID)
	// }

	// if product.Name != testCase.expectedResult.Name {
	// 		t.Errorf("Nome esperado: %s, Nome retornado: %s", testCase.expectedResult.Name, product.Name)
	// }

	// if err != testCase.expectedError {
	// 		t.Errorf("Erro esperado: %v, Erro retornado: %v", testCase.expectedError, err)
	// }
}
